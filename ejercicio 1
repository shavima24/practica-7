#include <stdio.h>

void main ()
{
    int a, b;
    printf ("dame un numero:");
    scanf("%i",&a);
    printf("dame otro numero:");
    scanf ("%i",&b);
    
        if(a > b)
    {
    printf ("a es mayor que b /n");
    }
    printf ("fin del programa /n");
}
